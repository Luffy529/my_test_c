#include <stdio.h>
#include <stdlib.h>

int main()
{
    int A, B;
        printf("entrer la valeur A : ");
        scanf("%d", &A);
        printf("entrer la valeur B : ");
        scanf(" %d", &B);
    if (A*B>0)
        printf("A et B ont la meme singe");
    else printf("A et B ont deux signes differents");





    return 0;
}